# Kaggle-optimized requirements for XTTSv2 Sinhala Finetuning
# Core dependencies
numpy>=1.24.3
torch==2.1.0
torchaudio==2.1.0
transformers==4.45.2
tokenizers==0.20.1

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0
scipy>=1.11.2

# Data processing
pandas>=1.4.0,<2.0
scikit-learn>=1.3.0
tqdm>=4.64.1

# TTS specific
trainer>=0.0.36
coqpit>=0.0.16
einops>=0.6.0
unidecode>=1.3.2
num2words
pysbd>=0.3.4

# Text processing
inflect>=5.6.0
anyascii>=0.3.0
pyyaml>=6.0

# Visualization
matplotlib>=3.7.0

# Optional: Remove problematic packages for Kaggle
# gruut[de,es,fr]==2.2.3  # Comment out if causing issues
# spacy[ja]>=3  # Comment out if causing issues
